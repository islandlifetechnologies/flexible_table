name: flexible_table
description: A table with flexible sized colums and an effectively unlimited number of rows that retains performance.
repository: https://github.com/islandlifetechnologies/flexible_table
version: 3.1.0

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  multi_split_view: ^3.6.2
  popover: ^0.4.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
